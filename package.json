{
  "scripts": {
    "build": "webpack --mode=production",
    "watch": "webpack --mode=production --watch --progress",
    "test": "npx vitest@^3.0.0 run",
    "test:watch": "npx vitest@^3.0.0"
  },
  "devDependencies": {
    "@octokit/rest": "^21.1.1",
    "vitest": "^3.0.0",
    "webpack": "^5.105.0",
    "webpack-cli": "^5.0.1"
  }
}
